global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "accident-api"
    static_configs:
      - targets: ["localhost:8000"]

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]